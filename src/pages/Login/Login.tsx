import React, {memo} from 'react';

import Layout from 'src/components/Layout';

function Login() {
  return (
    <Layout name="Login">
      <div>Login page</div>
    </Layout>
  );
}
export default memo(Login);
